<%= form_for @team do |f| %>
  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :name, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "salvar" %>
  </div>
<% end %>
